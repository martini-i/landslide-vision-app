# Production/deployment dependencies for the FastAPI backend + model inference.
# Does NOT include dev-only tools (gradio, scikit-learn, matplotlib) — those stay
# in the root requirements.txt for local training/evaluation work.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
numpy>=1.24.0
grad-cam>=1.5.0
transformers>=4.40.0
